[package]
name = "bevy-hello-a2"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"

[profile.dev]
opt-level = 1
debug = false # <--- This is the magic line that fixes the 1GB file size

[profile.dev.package."*"]
opt-level = 3
